# 安装执行列表中文标签实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将安装执行列表中的目标范围和执行方式显示为中文。

**Architecture:** 后端继续返回稳定的英文枚举值，前端通过只读映射函数转换为中文显示文本。未知枚举回退显示原值，避免空白信息。

**Tech Stack:** Vue 3、Element Plus、Vitest

## 全局约束

- 所有修改文件使用 Linux LF 换行符。
- `doc/` 和 `docs/` 目录下的文档文件使用中文名称。
- 不改变后端接口字段及安装执行逻辑。

---

### 任务 1：安装步骤中文显示

**Files:**
- Modify: `web/frontend/src/App.vue`
- Test: `web/frontend/src/App.test.js`

**Interfaces:**
- Consumes: 安装计划项的 `target_scope: string` 和 `mode: string`
- Produces: `installTargetText(value)` 与 `installModeText(value)` 显示函数

- [ ] **步骤 1：编写失败测试**

让 `getInstallPlan` 返回包含 `primary_control_plane`、`workers`、`serial`、`parallel` 的步骤，断言页面显示“主控制节点”“工作节点”“串行”“并行”。

- [ ] **步骤 2：运行测试并确认失败**

运行：`npm test -- --run src/App.test.js`

预期：中文标签断言失败，页面仍显示英文枚举。

- [ ] **步骤 3：实现最小映射**

在 `App.vue` 中增加目标范围、执行方式映射及回退函数，并让两个表格列调用对应函数。

- [ ] **步骤 4：运行测试和生产构建**

运行：`npm test -- --run src/App.test.js` 和 `npm run build`

预期：测试全部通过，生产构建成功。

- [ ] **步骤 5：检查格式**

运行：`git diff --check`，并检查本次修改文件不包含 CR 字节。

预期：命令退出码为 0，文件保持 LF。
