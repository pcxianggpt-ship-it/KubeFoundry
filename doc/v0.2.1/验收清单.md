# KubeFoundry v0.2.1 验收清单

## 已完成的自动化验收

- [x] Flyway 从 V1 全量迁移到 V8。
- [x] Flyway 从 V7 增量迁移到 V8，覆盖 Registry 角色迁移和历史安装锁修正。
- [x] 后端安装、预检查、快照、锁定、重置和活动任务准入测试。
- [x] 前端集群配置与集群安装路由、安装确认、重置强确认和任务执行测试。
- [x] 双架构发布包静态构建、校验和、部署目录与 `${APP_DIR}/kube-media` 检查。
- [x] LF、敏感信息和 Git 差异检查。

## 尚需真实环境验收

- [ ] 在专用 x86_64 测试集群完成安装成功、锁定、远程重置和解锁闭环。
- [ ] 在专用 ARM64 测试环境完成离线介质、服务启动和非破坏性预检查。
- [ ] 复核同机 Registry 与 Kubernetes 角色的真实清理顺序。

上述真实环境项目未完成前，v0.2.1 不应标记为双架构动态验收通过。
