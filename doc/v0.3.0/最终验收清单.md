# KubeFoundry v0.3.0 最终验收清单

## 自动化门禁

- [x] Maven 后端测试通过。
- [x] 前端 Vitest 测试与生产构建通过。
- [x] Phase3 组件 Bash 测试通过。
- [x] Java Web 冒烟测试通过。
- [x] 双架构发布包、篡改、越界符号链接和 Helm 缺失资源测试通过。
- [x] LF、敏感信息和 `git diff --check` 检查通过。

## 真实环境门禁

- [x] 5 台 x86_64 专用机器按“管理端 + 主控制节点 + Registry”同机、2 个其他控制节点、2 个 Worker 的拓扑完成应用启动、组件介质分发和非破坏性预检查。
- [x] 管理端与主控制节点同机时，本机 SSH/SCP、统一远程 kubectl/Helm 执行、任务临时介质复制和管理端目录保护均通过。
- [x] 内部固定 NFS 部署在指定 Worker，动态 PVC 读写、重复安装、失败重试及非受管 exports/fstab 配置和测试数据保留均通过。
- [x] 新集群全量安装、v0.2.1 存量集群补装、失败重试闭环完成。
- [x] 远程 reset 由 Sol 完成 SSH、受管资源清理和状态回写专项安全复核后，在专用环境验收。

ARM64 真实环境和 external NFS 真实环境不纳入本轮发布门禁。ARM64 代码、双架构包结构、`helm-arm` 映射和 external NFS 逻辑继续由自动化测试覆盖；v0.3.0 当前真实环境发布结论仅适用于 x86_64，不得宣称 ARM64 动态验收通过，获得 ARM64 环境后另行补验。

未完成上述 x86_64 真实环境门禁时，不得将本版本标记为生产发布完成。

Java Web 冒烟测试已于 2026-08-08 通过 Sol 专项分析和主模型复核。修复内容包括限制 Apache SSHD 客户端异常关闭等待时间、放宽节点任务终态轮询预算，以及消除健康接口和任务响应 JSON 字段顺序依赖；完整端到端冒烟、SSH 异常断连测试和节点测试服务回归均已通过。

### 2026-08-08 x86_64 真实环境记录

- Kubernetes 基础安装任务 3 成功，3 控制面 + 2 Worker 全部 Ready。
- NFS 组件任务 45 成功，使用内网镜像 `registry:5000/nfs/nfs-subdir-external-provisioner:v4.0.2`；动态 PVC 创建及 Pod 跨节点读写通过。
- 真实 reset 任务 46 成功：五机 kubelet 停止，Kubernetes/容器运行时受管目录清理，NFS exports/fstab 受管标记移除，NFS 共享数据的文件数、字节数和样本 SHA-256 保持不变，Registry 清理，管理 API 保持健康，集群回到 `draft` 且解锁。
- reset 的 Helm 卸载分支未通过真实验收：NFS release 因 reset 只读取基础安装快照的旧介质校验值而安全跳过，后续随 etcd 清理消失。源码已改为合并成功的组件安装快照并优先使用最新校验值，Java 全量测试和 reset 脚本回归已通过；下一次重装/reset 周期仍需补验真实 `helm uninstall`。

### 2026-08-09 x86_64 重装与 reset 回归

- 基础安装任务 48 成功：5 台节点 SSH 测试、14 个基础安装步骤和集群健康检查全部通过，三控制面和两台 Worker 均为 Ready。
- NFS 首次组件任务 49 按预期暴露离线镜像缺口：reset 后新建 Registry 没有 `registry:5000/nfs/nfs-subdir-external-provisioner:v4.0.2`，Provisioner 进入 ImagePullBackOff；未访问公网。
- 已增加 `32-import-nfs-image` 步骤，从 `/root/kube-media/03.setup_file/v1.30.14/helmapp/nfs/nfs.tar` 导入并推送至内部 Registry，并在 Helm 安装后滚动重启 Deployment。重试任务 50 的 6 个步骤全部成功，内部 Registry 标签和 Provisioner Running 均已实测。
- PVC 在 Worker 63 写入、Worker 62 读取，SHA-256 均为 `d3bc3ce9043f944d939ebddb8bdc04ae1eabe3d841ced643abf0857be628e6c0`。
- reset 任务 51 成功，日志明确记录 `release "nfs-subdir-external-provisioner" uninstalled`；五台 kubelet 停止、受管目录与 NFS 标记清理，NFS 共享样本、动态 PVC 数据、管理 API 与离线介质均保留。

### 2026-08-09 v0.2.1 存量集群补装与前端安全升级

- 使用 v0.2.1 包在全新验收数据目录创建同一 5 节点拓扑并完成节点测试；基础安装任务 2 在镜像仓库步骤因旧 reset 遗留的孤儿 `nerdctl0` 网桥占用默认 `10.4.0.0/24` 子网而失败。
- 使用新 v0.3.0 x86_64 包原地升级，Flyway 将存量数据库从 v9 自动迁移至 v12，历史节点与失败任务保留；任务 3 从失败状态重新提交后，14 个基础步骤全部成功，五节点均为 Ready。
- 已在 Registry 安装脚本中加入受限恢复：仅当没有 nerdctl 容器且默认网络不存在时，清理孤儿 `nerdctl0` 网桥；真实任务 3 的镜像仓库步骤验证通过。
- NFS 补装任务 4 的 6 个步骤全部成功，离线导入并使用 `registry:5000/nfs/nfs-subdir-external-provisioner:v4.0.2`，未访问公网；动态 RWX PVC 在 Worker 62 写入、Worker 63 成功读取 `v021-nfs-acceptance`，临时 PVC 与 Pod 已清理。
- 前端依赖升级至 Vite 6.4.3、Vitest 3.2.6、`@vitejs/plugin-vue` 6.0.8；Vitest 50 项测试和生产构建通过，`npm audit --package-lock-only` 为 0 漏洞。
