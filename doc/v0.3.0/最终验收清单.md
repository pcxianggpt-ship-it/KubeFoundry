# KubeFoundry v0.3.0 最终验收清单

## 自动化门禁

- [x] Maven 后端测试通过。
- [x] 前端 Vitest 测试与生产构建通过。
- [x] Phase3 组件 Bash 测试通过。
- [ ] Java Web 冒烟测试通过。
- [x] 双架构发布包、篡改、越界符号链接和 Helm 缺失资源测试通过。
- [x] LF、敏感信息和 `git diff --check` 检查通过。

## 真实环境门禁

- [ ] x86_64 环境完成应用启动、组件介质分发和非破坏性预检查。
- [ ] ARM64 环境完成应用启动、Helm 分发和非破坏性预检查。
- [ ] 新集群全量安装、v0.2.1 存量集群补装、失败重试闭环完成。
- [ ] 远程 reset 由 Sol 完成 SSH、受管资源清理和状态回写专项安全复核后，在专用环境验收。

未完成任何真实环境门禁时，不得将本版本标记为生产发布完成。

当前工作区的 Java Web 冒烟测试尚未通过：已确认 WSL 具备 Java 17 与 Node.js，但非登录 Shell 需要显式加载 `JAVA_HOME`、`NODE_HOME` 和 `PATH`。测试使用本地异常关闭的 SSH 连接时，Apache SSHD 记录 `Broken pipe` 后节点任务未在超时内进入终态；该 SSH 异常收敛问题需要 Sol 专项分析和回归验证。
