# KubeFoundry v0.3.1 最终验收清单

## 1. 当前结论

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 本地 Java 自动化 | 通过 | 2026-08-14：202 项通过，19 项按环境条件跳过 |
| 前端全量自动化 | 通过 | 2026-08-14：15 个测试文件、52 项全部通过，生产构建通过 |
| CoreDNS/NFS/Kubemate/Prometheus/存储日志脚本 | 通过 | 仓库伪命令测试全部通过 |
| V14 数据库迁移 | 通过 | 空库及历史版本增量迁移通过 |
| 家庭网络真实集群 | 通过 | 3 节点历史环境及 1 控制节点+4 Worker 新环境均完成验收；新环境 NFS 缺陷按要求忽略 |
| 最终发布结论 | 有条件通过 | P0/P1 功能阻断为 0；安全专项整改和第二操作者复现仍待完成 |

## 2. 已完成的契约补齐

- 任务增加 `created_at`、`started_at`、`finished_at`。
- 步骤增加 `status_reason`，跳过和失败原因可查询。
- 新增 `/cluster-install/:clusterId/jobs/:jobId` 规范页面路由和集群范围 API。
- 旧 `/jobs/:jobId/execution` 地址自动重定向到规范地址。
- 安装概览按任务 ID 倒序展示安装和组件补装记录。
- 跨集群任务访问返回 `CLUSTER_JOB_NOT_FOUND`。
- 任务切换使用请求序号丢弃迟到响应，并关闭旧 SSE。

## 3. 家庭网络验收结果

2026-08-14 在 `192.168.0.225/160/64` 执行，结果如下：

1. 3 个节点均为 Ready，Kubernetes v1.30.14；现有集群节点测试状态为 `success`。
2. 发现 CoreDNS 在 Flannel 就绪前误用 nerdctl `10.4.0.0/24` 网桥，导致跨节点 DNS 超时；重建后两个副本使用 `10.244` 网段，跨节点 Service 解析通过。安装脚本已增加自动检测和修复。
3. NFS Server export 正常，`k8sw2` 目标挂载仅 1 条；重复补装未叠加挂载。冲突和超时注入由仓库测试覆盖，未在真实集群破坏性执行。
4. Kubemate 管理组件运行正常。
5. 安装任务 6 验证组件组隔离：存储日志组失败后，独立的 Prometheus 组继续执行，任务收敛为 `partial_success`。
6. 重置和配置解锁已由此前任务 4 完成；本轮未再次破坏性重置。
7. Prometheus Worker 标签、数据目录、PV/PVC、Operator、Exporter、Metrics Server 和 `kubectl top nodes` 正常；Prometheus `2/2`、Alertmanager `3/3` 可用。
8. OpenEBS 重复补装幂等通过；MinIO Operator、MinIO Pod、Service 和 Worker 数据目录通过；Loki全部工作负载和Canary正常；Alloy三个节点均 `2/2 Running` 且近期日志无错误。
9. 任务 6～12 可分别查询；任务 12 在 54 秒内成功，全部七个步骤成功，组件状态转为 `installed`。`/cluster-config`、`/cluster-install`、`/cluster-install/1`、`/cluster-install/1/jobs/9` F5请求均返回 200 SPA入口。
10. 发现并修复 OpenEBS Helm release 重复安装失败、OCI image index误判、Loki sidecar仓库路径以及CoreDNS误用nerdctl网桥问题。

### 3.1 四 Worker 真实安装复验

2026-08-15 在 `192.168.0.225/160/62/63/64` 对安装任务 3 继续验收：

1. 1 个控制节点和 4 个 Worker 全部 Ready，CoreDNS 两副本使用 `10.244` 网段；Traefik 和 Node Exporter 均为 `5/5`。
2. 任务 3 为 `partial_success`。NFS 失败按要求忽略；Kubemate、Traefik、OpenEBS 和 Prometheus 成功，MinIO 因离线介质缺少 `minio-dev.yaml` 失败，Loki/Alloy按同组依赖跳过；Prometheus 独立组继续成功，组件组隔离通过。
3. 废弃单 Pod `minio-dev.yaml` 方案，改用 Operator v5.0.14、`minio.min.io/v2` Tenant 和本地 Kustomize 配置文件。
4. MinIO Tenant 为 `Initialized/green`；4 个非 root Server Pod 分布在 `k8sw1`～`k8sw4`，4 个 `openebs-hostpath` PVC 均 Bound，内部健康接口通过。
5. MinIO 重复安装后 Secret UID 和全部 PVC UID 不变，未重复创建卷或轮换凭据。
6. Loki `read 3/3`、`write 3/3`、`backend 3/3`、Gateway `1/1`；Alloy 和 Node Exporter 均 `5/5`；近期日志无 S3 认证或连接错误。
7. Prometheus `2/2`、Alertmanager `3/3`，五节点 `kubectl top nodes` 正常。
8. 正式补装任务 4 发现 Alloy ConfigMap 非幂等缺陷并完成修复；正式补装任务 5 中 OpenEBS、MinIO、Loki、Alloy全部成功，`storage_observability` 转为 `installed`。任务仅因已忽略的 NFS 收敛为 `partial_success`。
9. `/cluster-config`、`/cluster-install`、`/cluster-install/1`、任务 3 和任务 5 详情路由均返回 200 SPA 入口；任务 3、4、5 可按不同任务 ID 独立查询。
10. 使用 Tenant Secret 完成 S3 测试对象 PUT/GET/DELETE；重建一个 MinIO Server Pod 后对象仍可读取，PVC UID 保持不变，故障恢复通过。

## 4. 发布前门禁

- [ ] 家庭网络动态用例全部通过并保存脱敏证据。
- [ ] NFS、安装状态机和 MinIO 安全专项整改完成。
- [ ] MinIO 操作手册由第二位操作者复现。
- [x] 前端全量测试和生产构建通过。
- [x] `mvn clean test package` 通过；19 项跳过均为测试声明的环境条件。
- [x] 全部 Bash、LF、敏感信息和 Git 差异检查通过。
- [x] 生成 v0.3.1 x86_64 发布包并记录 SHA-256。
- [x] 发布包不包含 `.superpowers/`、本地 `dist/` 缓存、数据库、凭据或 `server.md`。
- [x] P0/P1 功能缺陷为 0。

安全复核进展：MinIO 已改为 Tenant Secret 管理凭据，Loki 安装时读取同一 Secret，Server Pod 使用非 root 安全上下文且只暴露集群内部服务。生产发布前仍需完成 TLS、凭据轮换演练、独立磁盘或生产 CSI、备份恢复、网络策略和容量规划专项；NFS 安全策略不计入本轮复验结论。

## 5. 真实环境记录

```text
Git 提交：
发布包 SHA-256：036bc090f6dc87534aaa838f93326c7d59dabb15f5ef867adcb580515b8c94e8
验收环境：家庭网络 192.168.0.225 / 192.168.0.160 / 192.168.0.64
验收日期：2026-08-14
集群 ID：1
任务 ID：6、7、8、9、10、11、12

节点与批量测试：通过（沿用安装前任务 1 的 success 结果）
CoreDNS：通过，两个副本位于10.244网段，跨节点DNS解析成功
NFS：通过
Kubemate 与失败隔离：通过
重置与配置解锁：沿用任务 4 结果，本轮未重复执行
Prometheus：通过，Prometheus 2/2、Alertmanager 3/3可用
OpenEBS：通过
MinIO：通过
Loki：通过，全部工作负载和Canary正常
Alloy：通过，三个节点均2/2 Running
任务历史与刷新：通过
安全复核：已发现生产阻断项，待专项整改

P0：0
P1：无新增未解决代码缺陷
最终结论：功能验收通过；安全专项整改和第二操作者复现完成后可签署最终发布
执行人：
复核人：
```
